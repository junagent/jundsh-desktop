# Changelog

## v1.5.0 (2026-08-26)

**新增：极致好用 P0 四件套（第十二轮）**

- **未读消息感知**：解析 DSH 网页标题的未读前缀（`(N)` / `（N）` / `【N】` / `[N]`，`src/unread.js` 单一事实源 + 7 例单测），主窗收进托盘后：
  - 任务栏图标叠加红色未读角标（`setOverlayIcon`）
  - 托盘 tooltip 显示「N 条未读」
  - 悬浮鲸鱼呼吸灯加速（`html.unread` 类，0.85s 脉冲）
  - 用户一聚焦主窗立即清零；正看着窗口时忽略新上报，不打扰
- **页内查找 `Ctrl+F`**：玻璃质感查找条（右上角），实时高亮 + `n/N` 计数、Enter/Shift+Enter 上下跳转、Esc 关闭清除选区；webview 内按键经 `before-input-event` 无缝转发
- **命令面板 `Ctrl+K`**：毛玻璃面板 + 子序列模糊搜索（前缀 > 词首 > 子序列打分排序）、↑↓ 选择回车执行；聚合 25+ 动作——设置/终端/刷新、DSH 服务启动·重启·停止、三外观×五皮肤即时切换（走 `applySettingsPatch` 同一校验通道并同步设置表单）、缩放三连、悬浮鲸/全局热键/服务通知开关、导入导出配置、检查更新、收起到托盘
- **托盘状态色图标**：托盘白鲸右下角动态状态点——在线=深海青蓝 / 托管尝试中=琥珀 / 离线=灰红（`gen-icons` 扩展生成 online/connecting/offline 三态 @1x/@2x，状态切换按 key 去重防闪烁）
- 新增 `app:check-update` IPC（命令面板与托盘共用检查更新逻辑）

## v1.4.0 (2026-08-25)

**新增：深海 ABYSS 默认视觉（黑蓝深海 × 玻璃 UI × ASCII 原子像素）**

- **深海 ABYSS 皮肤（新默认）**：深渊层底色 `#060b14→#10203a`（越往下越暗）+ 生物荧光青蓝主色 `#4cc2ff`；加载遮罩与离线页新增**双层海雪粒子**（CSS 视差漂移，`prefers-reduced-motion` 自动停止）
- **玻璃 UI 升级**：标题栏/状态胶囊/按钮/终端抽屉全面玻璃化——更透的底色 + `backdrop-filter` 增强 + 顶部发丝高光（inset 高光线）；旋转渐变边框色 token 化为 `--rim-a/--rim-b`，随皮肤联动
- **ASCII 原子像素引擎 `src/atom.js`**：纯 ASCII 字符画原子（三椭圆轨道 + 电子逐帧运行，24 帧无缝循环）；浏览器挂载 API `JAtom.mount(el, {fps})`——系统「减少动态效果」时自动定格、页面隐藏时暂停省电；Node 端可 require 用于测试
  - 应用位置：启动页水印（低透明度大字）、离线页签名位、加载遮罩、内置终端首开占位符
- **皮肤体系重构**：`abyss`(新默认) / `graphite`(原深蓝改名保留) / 极光紫 / 翡翠 / 琥珀；存量用户设置中的 `'default'` 键一次性迁移为 `abyss`；石墨蓝皮肤完整恢复 v1.3 配色
- **悬浮鲸鱼跟随**：桌面悬浮鲸鱼菜单默认换深海青蓝描边，鲸鱼光晕 token 化随皮肤切换
- 启动页换装深海配色 + ASCII 菊花轮换 caption；窗口背景色同步深海底色（消除启动闪白）
- `npm run check` 覆盖新增的 `atom.js` / `splash.js`

**工程质量与可维护性（第十轮追加）**
- **GitHub Actions CI 质量门禁**：新增 `.github/workflows/ci.yml`——每次 push / PR 跑语法检查 + ESLint + 单测（秒级，不打包）；打包发布工作流在构建前先过同一道门禁，坏提交进不了 Release
- **ESLint 9 扁平配置**：只查正确性（未定义变量、不可达代码等），不掺格式意见——格式交给编辑器级 Prettier（`.prettierrc.json`，可选使用）；顺手清掉 float.js 三处未使用变量与 dsh-svc.js 死赋值
- **单元测试落地**：`node:test` 内置测试器 + `tests/` 目录，15 例覆盖 ASCII 原子渲染器（画布尺寸恒定 / 首尾无缝循环 / 电子逐帧运动 / 渲染确定性 / 越界索引安全）与设置 Schema 全部规范化函数；`npm test` 一键运行
- **设置 Schema 单一事实源 `src/settings-schema.js`**：皮肤/主题/DSH 模式白名单与 URL、缩放、端口的规范化收敛一处（UMD：主进程 `require`，外壳与悬浮窗挂 `window.JSchema`）——主进程 loadSettings、设置补丁校验、shell.js、float.js 四处重复实现全部删除，此后新增设置字段只改这一个文件
- **调试探针模块化 `src/debug-harness.js`**：原 main.js 内 ~180 行截图 / DOM 探针代码独立成模块（`DSH_DESKTOP_SHOT=1/modal/float` 三种模式不变），只在调试路径加载，常规运行时零开销
- **崩溃可见性**：主进程启动 crashReporter（转储仅本地留存，不上传）；「环境诊断」报告新增「崩溃转储」一行（目录 + 数量），用户反馈问题时可一键附上
- **便携版迁移引导**：便携版（无法自动更新）首次运行一次性弹窗提示迁移到安装版，「前往下载」直达 Releases；设置记忆，只提醒一次

**快捷与韧性（第十一轮追加）**
- **全局快捷键呼出/隐藏主窗**：任何界面按 `Ctrl`+`Alt`+`J` 一键唤出 / 收起 JUNDSH（注册失败自动降级并 toast 提示占用）；设置面板「常规」区可关闭
- **开机自启静默启动**：自启项带 `--hidden` 参数写入——登录 Windows 后小鲸鱼在托盘待命，不再弹窗打扰；点托盘 / 快捷键随时唤出
- **服务状态系统通知**：主窗收进托盘时，DSH 服务上线 ↔ 掉线转换弹系统通知（边缘触发不轰炸，首次探测只记基线），点击通知直达主界面；正看着主窗时不打扰
- **设置导入 / 导出**：「常规」区新增导出 / 导入按钮——导出剔除设备相关字段（窗口与悬浮鲸位置、自启状态等）的 JSON 备份；导入走 `pickImportable` 白名单 + 与保存完全相同的补丁校验路径，非法字段静默丢弃、外壳视觉与连接即时跟随
- **重构**：设置补丁应用逻辑收敛为主进程唯一入口 `applySettingsPatch`（UI 保存与备份导入共用），消除双路径漂移风险；设置表单填充收敛为 `fillSettingsForm`（顺带修复重开设置时主题 seg 高亮不刷新的旧问题）
- 单测扩至 19 例：新增 `pickImportable` 白名单过滤 / 规范化 / 设备字段剔除 / 非法输入用例

## v1.3.0 (2026-08-21)

**新增：DSH 服务管理（v2 第一阶段落地）**
- 新增 `src/dsh-svc.js`：DSH 服务管理器
  - 三种模式：`external`（外部运行，仅探测）/ `profile`（托管拉起已安装 Profile）/ `source`（托管拉起源码仓库）
  - 健康检查（HTTP 探测，5s 间隔 + 失败阈值）
  - 看门狗自动重启（限次/退避/串行化，防并发竞态）
  - 退出时自动停止托管子进程
- 设置面板新增「DSH 服务管理」区块：模式选择 / 端口 / 源码目录 / 启动·重启·停止按钮 / 实时服务状态
- 标题栏状态胶囊展示 `端口 · 模式 · 运行时长`
- 托管模式下 targetUrl 自动与所选端口联动，避免 webview 与服务端口错位
- 新增 `npm run smoke`（`scripts/smoke-dsh-svc.js`：拉起服务 + 看门狗自愈验证）

**其他**
- 冒烟/语法检查脚本纳入 `npm run check`

**新增：环境诊断（同日追加）**
- `src/diag.js`：一键采集本机环境（OS/Node/Electron/DSH Profile 路径与版本/源码目录/tsx/端口监听），自动发现问题清单
- 设置面板「环境诊断」区块：生成 + 复制报告

**新增：内置轻量终端（同日追加）**
- PowerShell 子进程经 IPC 双向串流（零原生依赖）
- 标题栏终端按钮 / `Ctrl`+` 切换；输出 + 命令回显 + 清屏；退出强杀子进程

**新增：桌面悬浮鲸鱼 + 皮肤系统（第三轮追加）**
- 官方黑色鲸鱼标识做成可置顶、可拖动、悬空的桌面小窗；单击菜单含实时连接状态；设置可开关
- 皮肤系统：accent token 化，深蓝/极光紫/翡翠/琥珀 四套预设，设置即时预览并持久化

**增强：悬浮鲸鱼交互 + 托盘/离线联动（第四轮追加）**
- 悬浮鲸鱼：屏幕边缘自动吸附成小条、悬停浮出；手动拖动经 IPC（点开菜单不受干扰）；深浅主题自动黑/白鲸切换；双击回主界面；右键菜单
- 托盘菜单新增「桌面悬浮鲸鱼」开关
- 离线页新增「一键启动服务」（托管模式直接拉起；外部模式引导去设置）

**修复与打磨（第五轮追加，自主审计落地）**
- **P0 关键修复**：DSH 托管模式（profile/source）在真实 Electron 运行中改用**系统 node.exe** 启动（与 start-dsh-web.ps1 一致，DSH HMR 无需特殊参数）；无系统 node 时回退 Electron 二进制 + `ELECTRON_RUN_AS_NODE=1` + `--expose-internals`。修复前该模式用 `process.execPath`（=electron.exe）启动必然失败。
- 清理死代码：`assetsDir`/`listenerPid`/`ensureTermAfterReload` 删除；`showFloatWindow` 复用为托盘「显示悬浮鲸鱼」恢复入口
- 托盘悬浮开关与设置面板状态同步（修改后即时刷新托盘菜单）
- 设置「恢复默认」立即真实落盘（不再需要再点「完成」）
- 悬浮窗 IPC 增加 sender 校验 + 坐标 clamp（防越界拖出屏幕）
- 运行时长格式化支持超过 24h 显示 `Nd Xh`

**体验打磨（第六轮追加）**
- 内置终端：**命令历史**（↑/↓ 遍历、不重复入栈、空命令不入）、状态栏显示**工作目录 cwd**、`Ctrl+L` 清屏快捷键
- 桌面悬浮鲸鱼：**吸附状态记忆**（重启后恢复上次吸附边并归位）、悬浮窗菜单**跟随皮肤 accent 协调**（极光紫/翡翠/琥珀 下菜单高亮/边框/状态点换色）
- 开发模式启动时提示「不检查自动更新」（toast + 日志），消除困惑

**深度细节打磨（第七轮追加，系统化审查）**
- **修复隐藏 bug**：设置面板「DSH 服务」模式 seg 高亮失效（`setDshMode` 参数错位导致托管样式/提示被误触发）；现已正常高亮并回写
- 首次启动悬浮鲸鱼**默认吸附右边缘缩条**登场（有历史位置则保持）
- 悬浮窗吸附到右边缘时**菜单自动向左展开**（防溢出屏幕）
- 设置弹窗打开时：标题栏双击最大化、`Ctrl±/0` 缩放快捷键**防误触**；`Esc` 支持**逐层关闭**（设置→终端）
- 终端打开时 toast 上移不被遮挡；离线页长 URL 截断防撑破卡片
- README 快捷键表格补全（终端开合/历史/清屏）

**体验与性能打磨（第八轮追加）**
- **跨显示器吸附修正**：拖动中按指针所在屏幕实时刷新吸附基准（600ms 节流），跨屏拖动吸附不再错位
- **诊断采集性能**：单次采集内缓存端口/版本探测，避免重复启动子进程
- **设置面板分区化**：外观 / 常规 / DSH 服务 / 诊断 四组标题组织，提升可用性
- **外壳崩溃自恢复**：shell 渲染进程意外退出自动 reload（限次防止崩溃循环）
- **toast 时序**：托管模式保存时统一提示（不再被"设置已保存"覆盖）

**状态可见性贯穿（第九轮追加）**
- **悬浮鲸鱼光效随连接状态**：在线=品牌蓝光呼吸 / 托管尝试中=琥珀脉冲 / 离线=灰红微光
- **托盘动态状态行**：`● DSH 在线 · 8080 · 12m` / `○ DSH 离线 · 8080 · 错误信息`，状态变化自动刷新
- **端口占用智能诊断**：抓取首页匹配 DSH 官方 Web UI 签名（`__ModuleLoader__` 等），区分「DSH 正常服务 / 被其他程序占用 / 未监听」，给出明确修复指引
- **更新下载进度 toast**：下载每 5% 提示一次（节流），完成通知保留

## v1.2.0 (2026-08-20)

- 高 DPI 托盘图标 @2x + 开机自启状态实时化
- 浅色模式卡片/Toast 可读性修复
- 深复检修复（便携版更新/错误提示/细节清理）
- 界面升级为 DeepSeek Harness 官网风格（品牌蓝 #679efe / 玻璃拟态 / 旋转渐变边框）

## v1.1.0

- 见 Git 历史（`git log --oneline v1.1.0..v1.2.0`）
